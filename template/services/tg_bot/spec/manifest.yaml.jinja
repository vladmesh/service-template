# Service manifest for tg_bot
# Declares dependencies on other services for client generation
{% if 'backend' in modules %}

consumes:
  - service: backend
    domain: users
    operations:
      - create_user
      - get_user
{% else %}

consumes: []
{% endif %}
