# Environment defaults for the tooling container (make lint / make tests)
# Auto-generated by copier. Edit freely; copier update won't overwrite.
ENVIRONMENT=test
APP_ENV=test
{%- if 'backend' in modules %}
POSTGRES_DB={{ project_slug }}_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/{{ project_slug }}_test
{%- endif %}
