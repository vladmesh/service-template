# Infrastructure contract

The canonical generated-project infrastructure contract lives in
[`template/infra/README.md`](../template/infra/README.md).

Read that file before generating a project when you need the worker-mode
contract for external orchestrators.
