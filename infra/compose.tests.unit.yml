services:
  # >>> services (auto-generated from services.yml)
  backend-tests-unit:
    image: ${BACKEND_IMAGE:-service-template-backend:latest}
    build:
      context: ..
      dockerfile: services/backend/Dockerfile
      args:
        INSTALL_DEV_DEPS: "true"
    command: pytest -q services/backend/tests/unit
    working_dir: /app
    env_file:
      - ../.env
    environment:
      PYTHONPATH: /app
      ENVIRONMENT: test
      APP_ENV: test
      DATABASE_URL: sqlite+pysqlite:////app/services/backend/tests/.tmp/test.db

  tg-bot-tests-unit:
    image: ${TG_BOT_IMAGE:-service-template-tg:latest}
    build:
      context: ..
      dockerfile: services/tg_bot/Dockerfile
      args:
        INSTALL_DEV_DEPS: "true"
    command: pytest -q services/tg_bot/tests/unit
    working_dir: /app
    env_file:
      - ../.env
    environment:
      PYTHONPATH: /app

  notifications-worker-tests-unit:
    image: ${NOTIFICATIONS_WORKER_IMAGE:-service-template-notifications-worker:latest}
    build:
      context: ..
      dockerfile: services/notifications_worker/Dockerfile
      args:
        INSTALL_DEV_DEPS: "true"
    command: pytest -q services/notifications_worker/tests
    working_dir: /app
    env_file:
      - ../.env
    environment:
      PYTHONPATH: /app

  test-service-tests-unit:
    image: ${TEST_SERVICE_IMAGE:-service-template-test-service:latest}
    build:
      context: ..
      dockerfile: services/test_service/Dockerfile
      args:
        INSTALL_DEV_DEPS: "true"
    command: pytest -q services/test_service/tests
    working_dir: /app
    env_file:
      - ../.env
    environment:
      PYTHONPATH: /app
  # <<< services (auto-generated from services.yml)

  tooling:
    image: ${TOOLING_IMAGE:-service-template-tooling:latest}
    build:
      context: ..
      dockerfile: tooling/Dockerfile
    working_dir: /workspace
    volumes:
      - ../:/workspace:delegated
    env_file:
      - ../.env
