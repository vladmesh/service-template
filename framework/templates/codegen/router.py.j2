# AUTO-GENERATED BY SERVICE-TEMPLATE - DO NOT EDIT
"""FastAPI router for {{ name }}."""

from __future__ import annotations

from collections.abc import Callable

from fastapi import (
    APIRouter,
{% if needs_body %}
    Body,
{% endif %}
    Depends,
{% if needs_path %}
    Path,
{% endif %}
{% if needs_query %}
    Query,
{% endif %}
)
{% if needs_broker %}
from faststream.redis import RedisBroker
{% endif %}
from sqlalchemy.ext.asyncio import AsyncSession
{% if param_type_imports %}

{% for imp in param_type_imports %}
{{ imp }}
{% endfor %}
{% endif %}

{% if imports %}
from shared.generated.schemas import (
{% for model in imports | sort %}
    {{ model }},
{% endfor %}
)
{% endif %}

from ..protocols import {{ protocol_name }}


def create_router(
    *,
    get_session: Callable[..., object],
    get_controller: Callable[[], {{ protocol_name }}],
{% if needs_broker %}
    get_broker: Callable[[], RedisBroker],
{% endif %}
) -> APIRouter:
    """Create the {{ name }} API router."""
    router = APIRouter(
        prefix="{{ router_prefix }}",
        tags={{ router_tags | list }},
    )

{% for handler in handlers %}
    @router.{{ handler.http_method | lower }}(
        "{{ handler.path }}",
{% if handler.output_model %}
        response_model={{ handler.computed_return_type }},
{% endif %}
        status_code={{ handler.status_code }},
    )
    async def {{ handler.name }}(
{% for param in handler.params %}
        {{ param.name }}: {{ param.type }} = {{ param.fastapi_source }},  # noqa: B008
{% endfor %}
{% if handler.input_model %}
        payload: {{ handler.input_model }} = Body(...),  # noqa: B008
{% endif %}
        session: AsyncSession = Depends(get_session),  # noqa: B008
        controller: {{ protocol_name }} = Depends(get_controller),  # noqa: B008
{% if handler.publish_channel %}
        broker: RedisBroker = Depends(get_broker),  # noqa: B008
{% endif %}
    ) -> {{ handler.computed_return_type }}:
{% if handler.output_model %}
        result = await controller.{{ handler.name }}(
{% else %}
        await controller.{{ handler.name }}(
{% endif %}
            session=session,
{% for param in handler.params %}
            {{ param.name }}={{ param.name }},
{% endfor %}
{% if handler.input_model %}
            payload=payload,
{% endif %}
        )
{% if handler.publish_channel %}
        await session.commit()
        await broker.publish(result, "{{ handler.publish_channel }}")
{% endif %}
{% if handler.output_model %}
        return result
{% else %}
        return None
{% endif %}

{% endfor %}
    return router
