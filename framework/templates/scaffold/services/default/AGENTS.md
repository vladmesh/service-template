# AGENTS — __SERVICE_NAME__

- Этот сервис создан через scaffold-шаблон. Не удаляйте и не переименовывайте базовые каталоги `src/` и `tests/`.
- Чтобы добавить новый сервис: добавьте запись в `services.yml` и создайте `services/<name>/` из шаблона `.framework/framework/templates/scaffold/services/<type>/`.
- Любые команды запускайте через `make` (см. `CONTRIBUTING.md`).
- После наполнения отредактируйте этот файл под конкретный сервис.
